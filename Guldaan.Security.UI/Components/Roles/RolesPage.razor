@inherits Basepage
@page "/roles"

@using Guldaan.Common.RazorUI.Authorize;
@using Guldaan.Common.RazorUI.Components;
@using Guldaan.Security.UI.Client.Components.Roles;

<PageTitle>Manage roles</PageTitle>

<UbikContentContainer>
    <UbikFluentBreadcrumb Items="BreadcrumbItems" WithClose="false" />

    @if (IsMegaAdmin())
    {
        <Roles @rendermode="InteractiveAuto"></Roles>
    }
    else
    {
        <NoRight />
    }
</UbikContentContainer>